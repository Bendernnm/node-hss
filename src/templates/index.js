const fileNotFound = require('./file-not-found');
const wrongFileFormat = require('./wrong-format');
const notAllowedMethod = require('./not-allowed-method');

module.exports = {
  fileNotFound,
  wrongFileFormat,
  notAllowedMethod,
};
